# Dependencies
node_modules/

# Build output (esbuild bundle; clasp pushes it, git doesn't track it)
dist/

# clasp credentials — never commit. (.clasp.json IS committed here, on purpose:
# it holds only the scriptId + push config, no secrets.)
.clasprc.json

# Real Pi/ngrok secrets and pulled live configs (see pi/README.md)
/private/

# Logs
*.log

# Secrets and local credentials (.env.example stays tracked).
.env
.env.*
!.env.example

# Claude Code working files (also global; repeated so clones get it too).
.claude/
CLAUDE.md
